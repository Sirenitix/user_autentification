create table Account (
                         id INT,
                         clientID VARCHAR(50),
                         bankID INT,
                         balance  DECIMAL(5,2),
                         withdrawAllowed VARCHAR(50),
                         accountType VARCHAR(50)
);